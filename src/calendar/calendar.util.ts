// Placeholder for BS/AD conversion logic
// In production, use a reliable Nepali date library or API

export class CalendarUtil {
  // Convert AD (YYYY-MM-DD) to BS (string)
  static adToBs(adDate: string): string {
    // TODO: Replace with real conversion
    return `BS(${adDate})`;
  }

  // Convert BS (string) to AD (YYYY-MM-DD)
  static bsToAd(bsDate: string): string {
    // TODO: Replace with real conversion
    return `AD(${bsDate})`;
  }
}
